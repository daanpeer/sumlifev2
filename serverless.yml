service: sumlife
plugins:
  - serverless-webpack

custom:
  webpackIncludeModules: true

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: eu-west-1
  memorySize: 128
  timeout: 10

functions:
  sumlife-set-webhook:
    handler: index.setWebhook
    environment: ${file(./env.yml)}
  sumlife-ask-questions:
    handler: index.askQuestions 
    environment: ${file(./env.yml)}
    events:
      - schedule: rate(30 minutes)
  sumlife-api:
    handler: index.api
    environment: ${file(./env.yml)}
    events:
      - http:
          path: api/{token}
          method: get 
          request: 
            parameters: 
              paths: 
                token: true
  sumlife-webhook:
    handler: index.webhook
    environment: ${file(./env.yml)}
    events:
      - http:
          path: webhook/{token}
          method: post
          request: 
            parameters: 
              paths: 
                token: true
